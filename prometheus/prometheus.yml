global:
  scrape_interval: 5s
  evaluation_interval: 5s

# Railway 단일 컨테이너 환경 설정
scrape_configs:
  # FastAPI 메트릭 (Prometheus Instrumentator)
  - job_name: "fastapi"
    static_configs:
      - targets: ["localhost:8000"]
    metrics_path: "/metrics"
